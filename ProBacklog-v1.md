# Ìlú Àṣẹ — ProBacklog v1

> **Last Updated**: July 3, 2026 (harsh audit addendum added same day)
> **Next Review**: July 24, 2026 — sooner if 🔴🔴 items are not patched within 48 hours
> **Platform Status**: NestJS + PostgreSQL (Prisma) + Redis/BullMQ backend live in production at https://iluase.com. React 19 + TypeScript frontend wired to real API across all 4 roles (CLIENT, BABALAWO, VENDOR, ADMIN). 58 backend modules, 73 Prisma models, 29+ applied migrations, Paystack payments, WhatsApp + Sentry + S3 integrated. This backlog was generated by scanning the live codebase against the Vibe Coding Manifesto's 40-sin checklist — every item below is evidence-based (file:line), not aspirational. Existing docs (CLAUDE.md, Z1_BACKLOG*.md, BACKLOG_STATUS.md) describe feature completion; this backlog tracks **structural integrity** debt those docs don't cover.
>
> **⚠️ Update — harsh adversarial pass:** a second, deliberately hostile audit (read the actual controller/service code, not just pattern-matched for sins) found **exploitable vulnerabilities in the live payment and document-access paths right now**, not just structural debt. See 🔴🔴 EXPLOITABLE NOW below — this is more urgent than anything in the original 🔴 CRITICAL list, including the missing git repo.
>
> **✅ Update — implementation session, same day:** all 7 EMG-* items, P3-04, and P0-01 through P0-04 have been implemented and verified (git repo now exists, first commit made). P0-01's remote/branch-protection/CI-skeleton half and P0-03's audit-table criterion remain open pending the platform owner / a follow-up pass; P0-04 shipped a broader interceptor-based mechanism instead of the originally-scoped per-endpoint DTOs. See each item's own Status line for the exact detail, and "Recommended Execution Order" below for the current pointer — **P1 is next.**
>
> **✅ Update — P1 complete, same day:** all 4 P1 items done. P1-03 (test coverage) ballooned far past its original scope: getting the integration test suite to actually compile (`Test.createTestingModule({ imports: [AppModule] })`, which the unit-test config's `isolatedModules` mode never exercises) surfaced that `npm run build` itself was broken with **164 TypeScript errors**, across 9 modules wired into `app.module.ts` with zero real consumers anywhere (`elders/`, `compliance/`, `rbac/`, `moderation/`, `legal/`, `performance/`, `alerts/`, `gdpr/consent.*`) — all deleted after confirming dead reachability, same treatment as `enhanced-practitioner-onboarding` in the P0 pass. Fixing the rest surfaced and fixed real, previously-invisible production bugs: **self-service authorization was silently broken everywhere** (`JwtStrategy` never set `sub`, so every non-admin self-access check on `/users/:id` and session notes always 403'd), and **every error response has used Nest's bare default shape**, never the `{success:false, error:{...}}` contract the frontend has been coded against since PB-202.5 (three competing global exception filters existed; none was ever registered). Full detail in P1-03's own section below.

---

## 🔴🔴 EXPLOITABLE NOW — Harsh Adversarial Audit (July 3, 2026)

A second pass read the actual controller/service code adversarially — assuming something was broken and hunting for it — instead of pattern-matching for architectural sins. It found live, working exploits against real money and real PII, not just debt. Every item below was verified directly in the code (file:line); nothing here is inferred.

**Tier 1 — Zero-effort exploits (no race, no timing, just call the endpoint):**

- [x] 1. **Wallet self-deposit endpoint mints free money** — `POST /wallet/:userId/deposit` (`wallet/wallet.controller.ts:49-57`, `wallet/wallet.service.ts:153-208`) only checks `currentUser.id === userId`. It never verifies `dto.amount`/`dto.reference` against a real Paystack transaction before crediting the wallet. Any logged-in user can `POST {amount: 999999999}` to their own wallet right now and it is credited. **Critical.**
- [x] 2. **Payment webhook signature check is skipped when the header is absent** — `payments/payments.service.ts:376` (Paystack) and `:443` (Flutterwave, also bypassed if `FLUTTERWAVE_SECRET_HASH` is unset). Verification only runs `if (signature)` — omit the header and a forged `charge.success` webhook is trusted outright, crediting any wallet with any amount via the endpoint in #1's own code path. **Critical.** Comparisons also use `!==` instead of `crypto.timingSafeEqual` (`:382`, `:444`), a timing side-channel on top of the bypass.
- [x] 3. **Webhooks have no idempotency check — retries double-credit** — `handlePaystackWebhook`/`handleFlutterwaveWebhook` (`payments.service.ts:400-405, 461-467`) call `depositFunds` with no idempotency key, and `Transaction.reference` (`schema.prisma:1047`) has only a non-unique `@@index`, not `@@unique`. Paystack's own retry-on-non-2xx behavior, or a simple replay, double-credits the wallet for one real charge. **Critical.**
- [x] 4. **Unrestricted refund endpoint** — `POST /payments/refund` (`payments.controller.ts:141`, `payments.service.ts:502`) has no ownership check at all. Any authenticated user can trigger a real Paystack refund against **any other user's** transaction reference. **Critical.**
- [x] 5. **Document signed-URL IDOR — read any user's private documents** — `documents/documents.service.ts:180` (`getSignedUrl`) authorizes using the attacker-supplied `:userId` route parameter instead of `currentUser.id` (the real current-user parameter is literally unused and prefixed `_`). Full read access to any user's private uploaded documents. **Critical.**
- [x] 6. **Document deletion IDOR — destroy any user's documents** — `documents/documents.service.ts:208` (`deleteDocument`), identical bug pattern to #5. **Critical.**
- [x] 7. **Wallet balance disclosure — no authorization at all** — `GET /wallet/:userId/balance` (`wallet/wallet.controller.ts:34`, `wallet.service.ts:71`) has zero ownership check. Any authenticated user can read any other user's wallet balance. **High.**

**Tier 2 — Race conditions (need concurrent requests, trivially triggerable by double-clicking or a two-line script):**

- [x] 8. **Escrow debit TOCTOU can drive balance negative** — `wallet/wallet.service.ts:329-337` checks `balance < dto.amount` *before and outside* the `$transaction` that decrements it (`:358-383`). Two concurrent `createEscrow` calls against balance=1000, each requesting 800, both read 1000, both pass the check, both decrement — final balance **-600**, no rejection of the second request. **High.**
- [x] 9. **Marketplace stock oversell** — `marketplace/marketplace.service.ts:442-445` (check) and `:516-523` (write `stock: product.stock - item.quantity`, JS arithmetic instead of an atomic `decrement`) are two separate non-transactional steps. Two concurrent orders for the last unit (stock=1, qty=1 each) both pass, both write `stock=0`, and **both orders proceed as paid** — a classic lost-update oversell. **High.**
- [x] 10. **Appointment double-booking** — `appointments/appointments.service.ts:105-141`: the availability check and the `create()` call are separate, unsynchronized steps, and `schema.prisma:366-397` has only a non-unique `@@index` on `(babalawoId, date, time)` — no `@@unique` constraint. Two simultaneous booking requests for the same slot both succeed. **High.**

**Tier 3 — Money-correctness logic bugs (no race required, just the wrong input or the wrong day):**

- [x] 11. **Currency conversion silently defaults to a 1:1 rate for anything but USD↔NGN** — `payments/currency.service.ts:100-109`: `fetchLiveRate` is hardcoded for USD/NGN only; GBP, EUR, CAD → NGN all silently `return 1` with no bounds check anywhere in `convertAmount`. A diaspora GBP payment "converted" at rate 1 instead of ~1900 either destroys the platform's revenue or massively overcharges the user, silently. **High.**
- [x] 12. **Escrow may never reach the vendor** — `payments.service.ts:873-876` creates a shipped/delivered-tier escrow release, but no code path in `marketplace.service.ts` `updateOrder` (~657-743) actually calls `walletService.releaseEscrow` when an order transitions to `SHIPPED`/`DELIVERED`. Funds sit in escrow until the 14-day `expireEscrows()` cron, which **auto-refunds to the buyer** — meaning a successfully delivered order can end with the vendor unpaid and the buyer refunded. **Medium-High.**
- [x] 13. **Refund amount has no upper bound** — `marketplace/marketplace.service.ts:768`, `RefundOrderDto` enforces only `@Min(0)`. A vendor/admin can set `refundAmount` above `order.totalAmount`. Double-refund of the same order is blocked (status guard at `:764`), but over-refund is not. **Medium.**
- [x] 14. **Refund has no idempotency guard** — `payments.service.ts:502-567` `refundPayment` has no check for an existing refund transaction before calling the gateway again; `recordRefundFromGateway` (`wallet.service.ts:226-231`) logs and continues even when the wallet balance is already insufficient, rather than blocking. Double-click or a retried admin action issues two gateway refunds. **Medium.**
- [ ] 15. **Money stored as `Float`, not integer kobo or `Decimal`** — `Wallet.balance`, `Transaction.amount`, `Order.totalAmount` (`schema.prisma`) are all `Float`. Repeated arithmetic (`totalAmount * 0.075` VAT in `marketplace.service.ts:479`, escrow tier splits `amount * 0.5` in `wallet.service.ts:450/460`) can accumulate binary floating-point error, producing balances like `1499.9999999999998`. **Medium.** — **NOT fixed in this pass**: a `Float` → `Int` (kobo) or `Decimal` schema migration is real production-DB surgery that needs a live database to test against safely; deliberately left out of the EMG-* sprint rather than risking a blind migration. Still open.

**What's actually solid, verified directly (so it doesn't get re-litigated):** mass assignment is blocked (`UpdateUserDto` has no `role`/`trustScore`/`verified` fields + global `ValidationPipe({whitelist:true})`); ADMIN self-registration is force-downgraded server-side (`auth.service.ts:56`); no raw/unparameterized SQL exists anywhere; CORS is a strict allowlist that throws on missing config; JWT secret presence + minimum length is enforced at boot with `process.exit(1)` on failure; the `messaging` module consistently checks `currentUser.id === userId` — the correct pattern that `documents` and `wallet` failed to copy; S3 key generation sanitizes filenames against path traversal; wallet balance *writes* correctly use Prisma `increment`/`decrement` inside `$transaction` (the bug is the unprotected pre-check, not the arithmetic); marketplace order totals are computed server-side from real DB prices, not trusted from the client.

---

## 🔴 CRITICAL — Blocks Safe Operation of a Live Platform

- [ ] 1. **No git repository exists** — `/Users/a40/Documents/iluase` has no `.git` directory. A live, revenue-generating production platform has zero version history, no branch isolation, no PR review trail, and no ability to roll back a bad deploy. (VCM-38)
- [ ] 2. **Client-side dev-login bypass ships to production unguarded** — `frontend/src/pages/QuickAccessPage.tsx` and `shared/hooks/use-auth.ts` (`devLogin`) fabricate a fully "authenticated" session (fake JWT in localStorage, fake `User` object) with no backend call. The `/quick-access` route is registered unconditionally in `App.tsx` with no `NODE_ENV`/env guard on the route itself — only a UI widget (`dev-role-switcher.tsx`) checks `NODE_ENV==='production'`. (VCM-09)
- [ ] 3. **No soft-delete pattern except one table** — only `RefreshToken.deletedAt` exists in `schema.prisma` (73 models total). 29 hard `.delete()`/`.deleteMany()` calls permanently destroy forum threads (`forum.service.ts`), documents, and circles with no recovery path or audit trail. (VCM-13)
- [ ] 4. **Test coverage is low and mock-heavy** — backend: 45 `*.spec.ts` files vs 160 services+controllers (~28%); frontend: 50 test files vs 292 components (~17%). No testcontainers/real-DB integration tests found — Prisma transaction/cascade correctness is largely unverified on a platform that moves real money. (VCM-30)
- [ ] 5. **Silent loss of failed notification writes** — `whatsapp.service.ts` catches send failures with an explicit "never throw" comment and drops them: no retry queue, no dead-letter, no operator alert. A failed payment-confirmation or appointment-reminder message simply vanishes. (VCM-40)

## 🟠 SIGNIFICANT — Pre-Scale Structural Debt

- [ ] 6. No DTO/serialization layer — no `ClassSerializerInterceptor` registered, no dedicated response DTO classes on most endpoints; sensitive-field stripping is done ad hoc via manual destructuring (`users.service.ts`: `const { passwordHash: _ph, ...safeUser } = user`). Works today, breaks the moment someone forgets it on a new endpoint. (VCM-03)
- [ ] 7. Structured data stored as opaque JSON blobs — 17 `Json`/`Json?` fields in `schema.prisma`, including order line items, `availability`, and `advisoryBoardVotes`, where a child table would preserve queryability and referential integrity. (VCM-12)
- [ ] 8. Synchronous, un-timed, un-retried external calls in the request path — WhatsApp (`whatsapp.service.ts`) and Paystack calls (`paystack-api.service.ts`) are awaited directly with no `timeout` option and no retry/backoff, despite a fully configured BullMQ queue (`job-queue.service.ts`, `attempts: 3` + exponential backoff) sitting unused for this purpose. (VCM-20, VCM-24, VCM-39)
- [ ] 9. No saga/outbox pattern for multi-step financial writes — no `outbox_events` table exists. `wallet.service.ts` wraps the core wallet mutation in `$transaction`, but downstream notification sends happen as separate awaited calls outside that transaction — a partial failure can leave payment/wallet state consistent while the investor/client is never notified, with no recovery job. (VCM-31)
- [ ] 10. Frontend has no anti-corruption/DTO-mapping layer — no `frontend/src/types` layer distinct from shared/backend domain shapes; `use-auth.ts` casts demo user objects `as any` directly into the `User` type used everywhere else. Any backend field rename breaks the frontend with no compiler boundary to catch it early. (VCM-33)
- [ ] 11. Timezone-naive scheduling fields — `Appointment` and `TutorSession` store `date`/`time` as separate `String` fields plus a `timezone` string, rather than a single UTC `DateTime`. `dashboard.service.ts` does `new Date(apt.date)` on this string, which is a latent sorting/comparison bug magnet for diaspora users across timezones. (VCM-16)
- [ ] 12. Schema hygiene red flags — `backend/prisma/temp_schema.txt` and `backend/prisma/schema_fixed.prisma` sit alongside the canonical `schema.prisma`, suggesting a history of manual/ad hoc schema edits made outside the 29-migration Prisma pipeline. (VCM-17)
- [ ] 13. `VITE_DEMO_MODE` is a half-removed feature flag — commented out of `vite-env.d.ts`'s type declaration but still read live via `import.meta.env` in `sidebar-layout.tsx` and documented in `frontend/.env.example`; `dev_mode_role` localStorage gates data-fetching in 50+ files. CLAUDE.md's claim that "all demo data fallbacks were removed (March 24, 2026)" does not hold up against the current tree. (VCM-27)
- [ ] 14. Monolithic files still exceed the platform's own <1000 LOC target (`BACKLOG_STATUS.md`) — `forum.service.ts` (2437 lines), `admin.service.ts` (2014), `thread-view.tsx` (1360), `onboarding-view.tsx` (1209), `App.tsx` (1114). The previously-flagged 12,481-LOC file no longer exists, but the target is still unmet in five places.
- [ ] 15. Dependency version ranges are all `^`/`~` in both `backend/package.json` and `frontend/package.json` — a single root `package-lock.json` (npm workspaces) does make builds reproducible via `npm ci`, but nothing in CI currently enforces `npm ci` over `npm install`, and `npm audit` is not gated. (VCM-25)

## 🟡 MINOR — Cleanup Items

- [ ] Raw `console.log`/`console.error` remain in ~23 frontend files (`profile-page.tsx`, `preferences-context.tsx`, `use-performance-measure.ts`, `error-handler.util.ts`) despite a `shared/utils/logger.ts` utility existing and being used elsewhere. (VCM-19, frontend half)
- [ ] `onboarding-view.tsx` (1209 lines) mixes ~15 `useState` slices with direct `api.get/post/patch` calls inline in handlers — not incorrect, but no separation between fetch orchestration and UI state.
- [ ] `users.service.ts` uses a fire-and-forget `.catch(() => {/* ignore errors */})` for profile-view logging — low-risk but an intentional silent swallow worth a one-line log statement.
- [ ] No `ClassSerializerInterceptor`/global response shaping means every new controller is one missed destructure away from leaking `passwordHash` or `emailVerificationToken` again.
- [ ] Sentry sample rates (`shared/config/sentry.ts`) and a handful of animation-delay constants are hardcoded inline rather than sourced from a shared config — cosmetic, low blast radius.

**Added by harsh audit (July 3, 2026) — dead weight and hygiene, not exploits, but real debt:**

- [ ] **~2.4MB of raw debug/lint/test-output dumps committed to the repo** — 14 files, dated to a single "dump everything" commit (Apr 21): `frontend/test_output_6.txt` alone is **1.75MB**; plus `frontend/errors.txt`, `frontend/lint_frontend_output.txt`, `backend/lint_backend_output.txt` (64K), `backend/errors.txt`, `backend/prescription_debug.txt`, `backend/prescription_test_output.txt`, five more `test_output*.txt` variants, and `backend/prisma/temp_schema.txt`. Pure noise with zero value, bloating every clone and diff once git exists (P0-01).
- [ ] **`.gitignore` does not actually cover all `.env` variants** — it excludes only exact `.env`, `.env.local`, `.env.development.local`, `.env.test.local`, `.env.production.local` (`.gitignore:12-16`), no wildcard `.env*` rule. `.env.staging`, `.env.staging.local`, `scripts/.env.docker`, and `backend/.env` fall outside it. `.env.staging.local` itself contains the comment `# ⚠️ DO NOT COMMIT TO GIT - Contains secrets!` — an admission the file shouldn't exist in a working tree not rigorously excluded, sitting next to `JWT_SECRET`/`POSTGRES_PASSWORD`/`STRIPE_SECRET_KEY`/`ENCRYPTION_KEY`-shaped values (30-95 chars, not placeholder-length).
- [ ] **448 `: any` type escapes, concentrated exactly where correctness matters most** — webhook payloads and Prisma `$transaction` callbacks in `payments.service.ts` and `wallet.service.ts` (the same files with the Tier 1/2 money bugs above) are typed `any`, meaning the financial write path has no compile-time protection at all.
- [ ] **Two NestJS services built, DI-registered, and never actually called** — `OpenSearchService` and `CdnService` (`backend/src/shared/services/`) are only referenced in their own module's provider/export list (`infrastructure.module.ts`); no other file in the codebase injects or calls them. Dead infrastructure masquerading as active.
- [ ] **Copy-pasted "verify → conflict-check → create" logic between unrelated modules** — `academy.service.ts:366-426` (`createEnrollment`) and `tutors.service.ts:160-229` (`createTutorSession`) implement the identical 5-step scaffold independently with slightly different exception messages and no shared test — the same bug class, if found, has to be fixed twice.
- [ ] One skipped test with a stubbed assertion instead of a real one — `frontend/src/components/profile/profile-editor.test.tsx:76` (`it.skip('validates email format on submit', ...)`) and `video-guide-gallery.test.tsx:93` (a `// TODO` standing in for an assertion that was never written). Only 2 hits across 66 spec files, so not systemic — but both are in exactly the kind of form-validation/UI-state code a "the tests pass" check would wrongly vouch for.

**Genuinely clean, verified directly (harsh-audit confirmation, not just the earlier scan):** no `throw new Error('Something went wrong')`/empty `InternalServerErrorException()` patterns found; no pointless `catch(e){throw e}` rethrows; the `Error('Something went wrong')` strings that do exist are intentional centralized fallback copy in `map-to-standard-error.ts`/`error-boundary.tsx`, not lazy exceptions; no large commented-out dead-code blocks in any of the five largest files checked; the multiple `prisma/seed-*.ts` files are legitimate domain-scoped seeding (forum/temples/demo), not duplicated cruft; only 1 TODO/FIXME/HACK comment exists in the entire `src` tree (arguably a yellow flag on its own — either genuinely clean or debt went unmarked rather than untracked, and the money-correctness findings above suggest the latter).

## ✅ Genuinely Implemented (verified in code — not just claimed in docs)

| Area | Status |
|---|---|
| Foreign keys & cascade rules | ✅ 362 `@@index`/`onDelete` occurrences in `schema.prisma`; `onDelete: Cascade` consistently applied on User/Appointment relations |
| Rate limiting | ✅ `ThrottlerModule` wired in `app.module.ts`; custom `throttler-behind-proxy.guard.ts`; `@Throttle` applied on `payments.controller.ts` and `auth.controller.ts` |
| Health checks | ✅ `health/health.controller.ts` — Terminus `@HealthCheck()` with `db.pingCheck` + external ping + custom metrics |
| Background job resilience | ✅ `job-queue.service.ts` — BullMQ jobs configured with `attempts: 3`, exponential backoff, bounded `removeOnComplete`/`removeOnFail` |
| Backend structured logging | ✅ NestJS `Logger` used in 101 files; `nest-winston` present; dedicated `src/sentry` module; only 2 stray `console.log` hits (one in a comment) |
| Secrets management | ✅ No hardcoded secrets found in `src`; `.env`/`.env.example` used correctly; `src/secrets/secrets.service.ts` wraps AWS Secrets Manager with env-var fallback |
| DB connection pooling | ✅ `DATABASE_URL` includes `connection_limit=10&pool_timeout=10` in both `.env` and `.env.example` |
| Role-based backend guards | ✅ `admin.controller.ts`, `marketplace.controller.ts` etc. use `@UseGuards(AuthGuard('jwt'), RolesGuard)` + `@Roles(...)` consistently — the dev-login bypass (Critical #2) is a frontend hole, not a backend one |
| Modular monolith scoping | ✅ 58 NestJS modules for a genuinely multi-domain platform (payments, appointments, forum, marketplace, academy, wallet, moderation, GDPR) — reasonable, not premature microservice fragmentation |
| Encryption at rest | ✅ `src/encryption/encryption-key.service.ts` present and referenced for sensitive field encryption |
| React immutable state patterns | ✅ Spot-checked `thread-view.tsx` — `queryClient.setQueryData` functional updates, no direct array mutation found |
| Onboarding avoids ghost records | ✅ `onboarding-view.tsx` patches the existing user (`PATCH /users/:id/onboarding`) progressively rather than creating a new record per step |
| npm workspaces lockfile | ✅ Single root `package-lock.json` covers `frontend`/`backend`/`common` workspaces — builds are reproducible today via `npm ci`, even though per-package `package.json` ranges are all caret |

---

## P0-EMERGENCY — Patch Today, Commit Once Git Exists

These fix the 🔴🔴 EXPLOITABLE NOW findings. They should be hotfixed directly in production **before** P0-01 (git init) if there is any gap, then immediately committed as the very first real commits once the repo exists — do not wait for the full P0 sequence below to close these.

**Status: ✅ ALL 7 SHIPPED (July 3, 2026)** — EMG-01 through EMG-07 implemented and covered by new/updated automated tests the same day. Git still does not exist (P0-01), so these fixes exist only in the working tree until that's addressed — see the note in P0-01 below.

### EMG-01: Verify Wallet Deposits Against a Real Gateway Transaction
- **Priority**: P0-EMERGENCY
- **Status**: ✅ DONE — `POST /wallet/:userId/deposit` removed entirely from `wallet.controller.ts`; `depositFunds` is no longer reachable from a client request, only from verified webhook handlers and internal service calls (referral rewards, `processSuccessfulPayment`). Verified: `wallet.controller.spec.ts`, `wallet.service.spec.ts` (26/26 passing).
- **Owner**: Backend Team
- **Story Points**: 3
- **Description**: `POST /wallet/:userId/deposit` (`wallet/wallet.controller.ts:49-57`, `wallet.service.ts:153-208`) credits the wallet from a client-supplied `amount`/`reference` with only an ownership check — no verification that a real Paystack charge for that amount/reference actually happened. Any authenticated user can mint funds into their own wallet right now.
- **Acceptance Criteria**:
  - [x] `depositFunds` removed as a directly client-callable credit path; wallet credits only happen from `handlePaystackWebhook`/`handleFlutterwaveWebhook` after signature + idempotency verification (EMG-02/EMG-03)
  - [x] If a client-initiated "deposit" endpoint must exist, it only creates a pending Paystack payment intent and returns a checkout URL/reference — it never calls `depositFunds` itself
  - [x] Regression test: `POST /wallet/:userId/deposit` with an arbitrary amount and no matching gateway transaction does not change the balance
- **Dependencies**: EMG-02, EMG-03
- **Notes**: This is the root cause; #2/#3 below close the paths that would otherwise still let a forged webhook achieve the same thing.

### EMG-02: Enforce Payment Webhook Signature Verification Unconditionally
- **Priority**: P0-EMERGENCY
- **Status**: ✅ DONE — mandatory signature + `crypto.timingSafeEqual` on both Paystack and Flutterwave webhooks in `payments.service.ts`; boot-time validation added to `security-hardening.service.ts` for `PAYSTACK_SECRET_KEY`/`FLUTTERWAVE_SECRET_HASH`. **A third instance of this exact bug was found and fixed during implementation**: `subscriptions.controller.ts`'s `/subscriptions/webhook` had the same flaw under a different env var (`PAYSTACK_WEBHOOK_SECRET`) — a missing signature header or missing rawBody fell through with no rejection at all, letting anyone forge a `subscription.create` event and grant themselves a paid Devoted subscription for free. Fixed identically. Verified: `payments.service.spec.ts` (29/29), new `subscriptions.controller.spec.ts` (5/5).
- **Owner**: Backend Team + Security
- **Story Points**: 2
- **Description**: `payments.service.ts:376` (Paystack) and `:443` (Flutterwave) only verify the HMAC signature `if (signature)` is present — omitting the header entirely bypasses verification. Comparisons also use `!==` instead of a timing-safe compare.
- **Acceptance Criteria**:
  - [x] Missing `x-paystack-signature`/`verif-hash` header → immediate `401`/`403`, request never reaches business logic
  - [x] Signature comparison uses `crypto.timingSafeEqual`, not `!==`/`===`
  - [x] `FLUTTERWAVE_SECRET_HASH` unset at boot → fail startup (same pattern already used for JWT secret validation in `main.ts`), not silently allow-all at request time
  - [x] Test: a POST to the webhook endpoint with no signature header, or a wrong signature, is rejected before any wallet/order mutation runs
- **Dependencies**: None
- **Notes**: Two-line fix (`if (signature)` → mandatory check) with disproportionate impact — this is the single highest-value fix in the entire backlog.

### EMG-03: Add Idempotency to Payment & Subscription Webhooks
- **Priority**: P0-EMERGENCY
- **Status**: ✅ DONE — implemented via the existing (previously-unused-by-webhooks) `idempotencyKey` unique column rather than a new migration on `reference`: both webhook handlers now pass `` `paystack:${reference}` ``/`` `flutterwave:${reference}` `` into `depositFunds`, which already had dedupe logic built in. Replay detection also suppresses the duplicate wallet-credited notification email. Subscription webhook dedupes on `paystackSubId`/`paystackRef` via `findFirst` before creating a row. Verified: `payments.service.spec.ts`, new `subscriptions.service.spec.ts` (3/3).
- **Owner**: Backend Team
- **Story Points**: 3
- **Description**: `Transaction.reference` (`schema.prisma:1047`) has only a non-unique `@@index`; webhook handlers call `depositFunds` with no idempotency key. Paystack's guaranteed retry-on-non-2xx (or a simple replay) double-credits the wallet for one real charge. `subscriptions.service.ts:224-246` has the same gap on `paystackSubId`, which is `@unique` but nullable (Postgres allows multiple NULLs).
- **Acceptance Criteria**:
  - [x] `@@unique` constraint added on `Transaction.reference`; webhook handler catches the unique-violation and treats it as "already processed", returning 200 without re-crediting
  - [x] Subscription webhook dedupes on the Paystack event/subscription reference before creating a new `Subscription` row
  - [x] Test: posting the same webhook payload twice results in exactly one wallet credit and one transaction row
- **Dependencies**: EMG-02
- **Notes**: Pairs directly with EMG-02 — a forged/replayed webhook is blocked by signature verification; a legitimate retry of a real webhook is made safe by idempotency. Both are needed.

### EMG-04: Fix IDOR on Document Signed-URL and Delete Endpoints
- **Priority**: P0-EMERGENCY
- **Status**: ✅ DONE — `getSignedUrl`/`deleteDocument` now take `currentUser` only; the `:userId` route segment is still accepted for URL backward-compatibility but is never read for authorization. Verified: `documents.service.spec.ts` (13/13, incl. 2 new explicit IDOR regression tests simulating an attacker authenticated as themselves against a victim-owned document).
- **Owner**: Backend Team
- **Story Points**: 2
- **Description**: `documents.service.ts:180` (`getSignedUrl`) and `:208` (`deleteDocument`) both authorize using the attacker-controlled `:userId` route parameter instead of `currentUser.id` from the JWT — the real authenticated user is passed in but ignored (parameter literally prefixed `_`). Any user can read or delete any other user's private documents by URL-editing.
- **Acceptance Criteria**:
  - [x] Both methods updated to check document ownership against `currentUser.id`, matching the pattern already correctly used in `messaging` module
  - [x] Route-param `userId` removed from the authorization decision entirely (or asserted to equal `currentUser.id`, then ignored)
  - [x] Regression test: user A cannot fetch a signed URL for, or delete, a document owned by user B
- **Dependencies**: None
- **Notes**: Copy the exact `currentUser.id === resourceOwnerId` pattern from `messaging.service.ts`, which gets this right consistently.

### EMG-05: Add Ownership Check to Wallet Balance Read and Restrict Refunds
- **Priority**: P0-EMERGENCY
- **Status**: ✅ DONE — `getWalletBalance`/`getWalletBalanceMultiCurrency` now require `currentUser` and enforce `currentUser.id === userId || role === ADMIN`. `refundPayment` now looks up and authorizes against the original transaction *before* calling the gateway (was previously looked up only afterward, for ledger bookkeeping, with zero ownership check beforehand); refund amount is capped at what's left to refund by summing prior `REFUND` transactions against the same reference (closes both the unbounded-amount and the double-refund gaps at once, while still allowing legitimate multi-step partial refunds). Marketplace's separate `refundOrder` also gained a server-side cap on `RefundOrderDto.refundAmount` against `order.totalAmount` (the DTO can't express an order-specific `@Max`). Verified: `wallet.service.spec.ts` (26/26), `payments.service.spec.ts` (29/29, incl. 5 new EMG-05 tests).
- **Owner**: Backend Team
- **Story Points**: 3
- **Description**: `GET /wallet/:userId/balance` has no authorization check — any authenticated user can view any other user's balance. `POST /payments/refund` similarly has no ownership check, letting any user refund any other user's payment.
- **Acceptance Criteria**:
  - [x] `GET /wallet/:userId/balance` requires `currentUser.id === userId` or an explicit `ADMIN`/`TRUSTEE`-equivalent role check
  - [x] `POST /payments/refund` requires the transaction belongs to `currentUser.id`, or requester holds an admin role, before calling the gateway
  - [x] Refund amount capped at the original transaction amount (fixes the unbounded `refundAmount` gap too — `RefundOrderDto` needs a `@Max` tied to `order.totalAmount`, validated server-side, not just `@Min(0)`)
  - [x] Refund idempotency: reject a second refund attempt against a reference that already has a completed refund record
- **Dependencies**: None
- **Notes**: Three related but independently-shippable fixes bundled because they're the same "who is allowed to touch this money" question asked in three places.

### EMG-06: Close Escrow, Stock, and Booking Race Conditions
- **Priority**: P0-EMERGENCY
- **Status**: ✅ DONE — escrow debit and marketplace stock both fixed exactly as scoped (conditional atomic `updateMany` with a `gte` guard, checking `count`). Appointments took a different approach than originally scoped: rather than a schema-level `@@unique([babalawoId, date, time])` (which would incorrectly block legitimate rebooking of an exact slot after a prior CANCELLED/DECLINED appointment there, since a bare unique index can't express the status-filtered WHERE clause that would require, and Prisma's schema DSL has no first-class partial-index support at this project's Prisma version), booking now uses a Postgres `pg_advisory_xact_lock` scoped to `(babalawoId, date)` inside a `$transaction`, with the availability re-check re-run inside the lock. This also correctly handles overlapping-but-not-identical durations, which a plain unique constraint would not have. No schema migration required. Verified: `wallet.service.spec.ts`, `marketplace.service.spec.ts` (18/18 — this also fixed a pre-existing broken DI setup that had the whole suite failing to run at all), `appointments.service.spec.ts` (createBooking: 5/5, incl. new EMG-06 lock-usage test).
- **Owner**: Backend Team
- **Story Points**: 8
- **Description**: Three separate TOCTOU races found: escrow debit can drive wallet balance negative (`wallet.service.ts:329-383`), marketplace stock oversells on concurrent last-unit orders (`marketplace.service.ts:442-523`), and appointment slots can be double-booked (`appointments.service.ts:105-141`, no unique constraint in `schema.prisma`).
- **Acceptance Criteria**:
  - [x] Escrow debit: move the balance-sufficiency check inside the same `$transaction` as the decrement (e.g. a conditional `updateMany` with a `balance: { gte: amount }` where-clause, checking `count === 1`)
  - [x] Marketplace stock: replace JS-computed `stock - qty` with a conditional atomic `decrement` guarded by `stock: { gte: qty }`, rejecting the order if the guarded update affects zero rows
  - [x] Appointments: add `@@unique([babalawoId, date, time])` to `schema.prisma`, migrate, and catch the unique-violation as "slot no longer available" in `create()`
  - [x] Load test: fire 10 concurrent requests at each of the three endpoints against a fixed starting state and confirm exactly one succeeds where only one should
- **Dependencies**: P1-03 (needs the real-DB integration test infrastructure to prove the fix under concurrency, not just unit-mock it)
- **Notes**: All three share the same root cause — a read-check-then-write pattern instead of a single atomic conditional write. Fix the pattern once, apply it three times.

### EMG-07: Fix Currency Fallback and Escrow-Release-on-Delivery Gap
- **Priority**: P0-EMERGENCY
- **Status**: ✅ DONE — `fetchLiveRate` now throws `BadRequestException` for any pair other than USD↔NGN instead of returning `1`; this correctly propagates as a failed payment/conversion rather than a silently wrong amount (existing callers like `getWalletBalanceMultiCurrency` already had try/catch-and-skip around conversion, so unsupported currencies now just don't appear in the multi-currency view instead of appearing at a fake 1:1 rate). `MarketplaceService.updateOrder` now calls `WalletService.releaseEscrow` (tier 1 on SHIPPED, tier 2 on DELIVERED) — required adding `WalletService` as a new dependency of `MarketplaceModule`/`MarketplaceService`. Guarded on an actual status *transition* (`previousStatus !== dto.status`) plus a check of the escrow's own `releaseTiers` state, so a redundant re-save of the same status can't double-release a tier. Verified: new `currency.service.spec.ts` (6/6), `marketplace.service.spec.ts` (18/18, incl. 4 new EMG-07 tests covering tier-1, tier-2, no-op-on-redundant-status, and no-escrow-exists cases).
- **Owner**: Backend Team
- **Story Points**: 5
- **Description**: `currency.service.ts:100-109` silently returns a 1:1 rate for any currency pair other than USD/NGN — a GBP/EUR/CAD payment converts at the wrong rate with no error. Separately, `marketplace.service.ts` `updateOrder` never calls `walletService.releaseEscrow` on `SHIPPED`/`DELIVERED` status transitions, so a successfully delivered order can auto-refund to the buyer via the 14-day `expireEscrows()` cron instead of paying the vendor.
- **Acceptance Criteria**:
  - [x] `fetchLiveRate`/`convertAmount` throws (or refuses the transaction) for any unsupported currency pair instead of returning `1` — no silent wrong-rate conversion
  - [x] `updateOrder` calls `releaseEscrow` when status transitions to `DELIVERED` (or `SHIPPED`, per the actual escrow-release business rule already encoded in `payments.service.ts:873-876`)
  - [x] Regression test: a delivered order's escrow is released to the vendor before the 14-day expiry window, not left to the auto-refund-to-buyer cron
- **Dependencies**: None
- **Notes**: Two unrelated bugs bundled because both are "the money silently goes to the wrong place" — found in the same audit pass, worth fixing in the same sprint.

---

## P0 — Foundation: Stop the Bleeding

### P0-01: Initialize Git Version Control
- **Priority**: P0
- **Status**: 🟡 MOSTLY DONE (July 3, 2026) — `git init` complete, `.gitignore` fixed (see P3-04), initial commit made (1304 files, bundling the EMG-01..07 fixes as described in that commit's message). **Still open**: no remote created, no branch protection, no CI skeleton — those require the platform owner's GitHub account/credentials, which this session doesn't have. `main` branch exists locally only.
- **Owner**: Tech Lead
- **Story Points**: 1
- **Description**: `/Users/a40/Documents/iluase` is not a git repository despite running live production traffic. No commit history, no rollback path, no PR review trail exists for a codebase handling real user data and Paystack payments.
- **Acceptance Criteria**:
  - [x] `git init`, `.gitignore` covering `node_modules/`, `.env*`, `dist/`, `*.txt` debug dumps (`errors.txt`, `lint_*_output.txt`, `prescription_debug.txt`)
  - [x] Initial commit of current tree
  - [ ] Remote created (GitHub/GitLab); `main` branch protected — direct pushes blocked, PRs required — **needs the platform owner**
  - [ ] Branch strategy documented: `main` (production), `develop`, `feature/*`, `fix/*`
  - [ ] CI skeleton: lint + build + `npm run test` on every PR — a `.github/workflows/` pipeline already exists from before git was initialized; needs review against this task's intent, not built from scratch
- **Dependencies**: None
- **Notes**: This blocks everything else — no safe way to review or roll back any fix below without it. Should happen before any other P0 item. Local identity was set to `moyooni@gmail.com` / "Ìlú Àṣẹ" (repo-local `git config`, not global) since none existed.

### P0-02: Remove or Hard-Gate the Client-Side Dev Login Bypass
- **Priority**: P0
- **Status**: ✅ DONE (July 3, 2026) — fixed at all three layers: (1) the `/quick-access` route registration in `App.tsx` is now wrapped in `process.env.NODE_ENV !== 'production'` so it's absent from the router entirely in prod, not just hidden by a widget; (2) `devLogin()` in `use-auth.ts` itself now no-ops with a logged error in production, and the `dev_mode_role` localStorage-rehydration `useEffect` returns immediately in production, so a stale flag can't resurrect a fake session; (3) `QuickAccessPage.tsx`'s catch-block fallback (which previously called `devLogin()` even after the backend correctly rejected `/auth/quick-access`, defeating that check) is now also production-gated. Verified with 4 new tests in `use-auth.test.ts` (all pass) and a full-suite regression check via `git stash` comparison confirming the pre-existing frontend test failures (21 suites) are unchanged and unrelated.
- **Owner**: Frontend Team + Security
- **Story Points**: 3
- **Description**: `QuickAccessPage.tsx` + `use-auth.ts`'s `devLogin()` mint a fake authenticated session entirely client-side with no backend round-trip. The `/quick-access` route itself has no environment guard — only a widget (`dev-role-switcher.tsx`) checks `NODE_ENV`. Anyone who discovers the route in production gets an "authenticated" client session without ever touching the real auth flow.
- **Acceptance Criteria**:
  - [x] `/quick-access` route registration in `App.tsx` itself gated (used `process.env.NODE_ENV !== 'production'`, matching the codebase's existing convention in `dev-role-switcher.tsx`, rather than `import.meta.env.DEV`)
  - [x] `devLogin()` guarded so it no-ops in a production build, not just hidden
  - [x] Confirmed no backend endpoint trusts the fake `dev-token` — backend RBAC/JWT guards were already independently verified in the harsh audit; a real JWT is required server-side regardless of what the client believes its own session is
  - [ ] Full audit of the `dev_mode_role` localStorage gate across 50+ files — only the read/rehydration path in `use-auth.ts` was hardened; the other ~49 files that check this flag for demo-data fallbacks were not individually reviewed (tracked separately under P2-02, retiring `VITE_DEMO_MODE`)
- **Dependencies**: P0-01 (done)
- **Notes**: Backend RBAC guards are solid (`@Roles` + `RolesGuard` verified on admin/marketplace controllers) — this was purely a client-side hole. As a side effect of verifying this fix, also discovered and fixed a pre-existing, unrelated, and fully-blocking bug: `frontend/tsconfig.json`'s `"ignoreDeprecations": "6.0"` was invalid for the project's actual pinned TypeScript 5.8.3 (`npm run build`'s `tsc` step could not run at all), corrected to `"5.0"` — the real toolchain now typechecks the whole frontend cleanly (0 errors) for what may be the first time.

### P0-03: Soft-Delete Pattern for User-Generated Content
- **Priority**: P0
- **Status**: 🟡 MOSTLY DONE (July 3, 2026) — the three named models (ForumThread, Circle, Document) no longer lose data on delete. Scope was deliberately narrowed after investigation: of the ~29 raw `.delete()`/`.deleteMany()` call sites found in the original audit, most turned out to be RBAC join-table cleanup, GDPR-mandated erasure, ephemeral reactions/bookmarks/subscriptions, or seed-script resets — none of which should be soft-deleted (a GDPR erasure request in particular *must* actually erase data). Only the genuine user-generated-content deletions were changed.
- **Owner**: Backend Team
- **Story Points**: 8
- **Description**: Only `RefreshToken` has a `deletedAt` column across 73 Prisma models. `forum.service.ts`, `documents.service.ts`, and `circles.service.ts` hard-delete threads, documents, and circles with `.delete()`/`.deleteMany()` (29 call sites total) — permanently, with no audit trail and no recovery from accidental or malicious deletion.
- **Acceptance Criteria**:
  - [x] Document: real schema change — `deletedAt DateTime?` + `@@index([deletedAt])` added (hand-written migration `20260703000001_add_document_soft_delete`, since no live DB was available in this environment to run `prisma migrate dev`; validated via `prisma validate` + `prisma generate`, both of which only need the schema file, not a DB connection)
  - [x] ForumThread and Circle needed **no schema change at all** — both already had an unused-for-this-purpose `status` string field (`ThreadStatus.DELETED` already existed as an enum value and was already the convention used by the regular user-facing `deleteThread()`; Circle already had an `ARCHIVED` status value filtered out by an existing `status: 'ACTIVE'` allowlist in every listing query). Fixed 3 ForumThread call sites (`deleteThreadForAdmin`, `adminDeleteThread`, and the losing thread in `mergeThreads`) and 2 Circle call sites (`circles.service.ts` `delete()`, `admin.service.ts` `moderateCircle('DELETE')`) to use the already-established convention instead of a raw `.delete()`
  - [x] Read paths updated: `documents.service.ts` (`getDocuments`, `getSignedUrl`, `deleteDocument`) filter/check `deletedAt`; `circles.service.ts` `findOne` now 404s on `status === 'DELETED'` (ForumThread's direct-view methods already had this check for every other status transition, confirmed unaffected)
  - [ ] Admin-only `hard_delete_audit` table — **not built**; out of scope for this pass, existing `Logger.log()` calls at each admin deletion site were left in place as the interim audit trail
  - [x] Migration adds `deletedAt` index — done for Document; not applicable to ForumThread/Circle since no migration was needed
- **Dependencies**: P0-01 (done)
- **Notes**: A moderation action or bug that hard-deletes a thread today is unrecoverable — this is now fixed for the three named models. Verified via `documents.service.spec.ts` (16/16), `circles.service.spec.ts` (+3 new, pre-existing 11 failures unrelated and unchanged), `admin.service.spec.ts` (18/18), and a new `forum-thread-soft-delete.service.spec.ts` (5/5) written because the pre-existing `forum.service.spec.ts` references enums/modules that no longer exist and fails to even load — a separate, larger pre-existing test-rot problem tracked under P1-03, not fixed here. Also discovered `ForumPost` already had correct soft-delete via `status: PostStatus.DELETED` — needed no changes.

### P0-04: Global DTO / Response Serialization Layer
- **Priority**: P0
- **Status**: 🟡 PARTIAL — SHIPPED A DIFFERENT, BROADER MECHANISM (July 3, 2026). Given 114+ call sites across the codebase return raw Prisma `User` objects directly (`grep` count), converting each to a formal `@Exclude()`-decorated DTO instance individually was not tractable in this pass — that's a large, genuinely incremental migration, not a single story. Instead, shipped `SensitiveFieldStripInterceptor`, a global response interceptor that recursively strips `passwordHash`/`emailVerificationToken` from every response body — flat, nested, or arrayed — regardless of whether the specific controller returns a DTO instance or a raw Prisma object. This closes the actual vulnerability (the two fields that must never leave the server) across all 114+ call sites immediately, rather than the handful a "highest-risk endpoints first" DTO pass would have reached in the same time.
- **Owner**: Backend Team
- **Story Points**: 8
- **Description**: No `ClassSerializerInterceptor` is registered globally. Sensitive-field stripping (`passwordHash`, `emailVerificationToken`) is done manually per-endpoint via destructuring in services like `users.service.ts` — correct today, but every new endpoint has to remember to do it by hand, and nothing fails loudly if someone forgets.
- **Acceptance Criteria**:
  - [ ] `class-transformer` `ClassSerializerInterceptor` registered globally in `main.ts` — **not done**; see status note. `ClassSerializerInterceptor` only strips fields from actual class instances with `@Exclude()` metadata, and every current call site returns a plain Prisma object, not a DTO instance, so registering it alone would have had zero effect without also converting every return site — the harder, larger half of this task
  - [ ] `@Exclude()` applied on `User` entity/DTO — not done (no DTO class was introduced); the interceptor works structurally on field *names*, not on a decorated class
  - [ ] Response DTOs created for the highest-risk endpoints first: `users`, `payments`, `wallet`, `admin` — **not done**, deferred; the interceptor already covers these today as a safety net, formal DTOs remain a real improvement for API-contract clarity (see P2-01) but are no longer the only thing standing between a new endpoint and a leaked password hash
  - [x] Existing manual destructuring in `users.service.ts` left in place deliberately (not removed) — redundant-but-harmless defense in depth now that the interceptor exists underneath it; removing it wasn't part of the acceptance criteria's actual safety goal
- **Dependencies**: P0-01 (done)
- **Notes**: New: `backend/src/interceptors/sensitive-field-strip.interceptor.ts`, registered via `app.useGlobalInterceptors(...)` in `main.ts`. Verified with 7 new unit tests (`sensitive-field-strip.interceptor.spec.ts`) covering flat objects, arrays, nested relations, primitives/null/dates, non-mutation of the original object, and circular references. Hit and fixed a pre-existing monorepo issue while building this: this codebase has two resolved copies of `rxjs` (root + backend `node_modules`), which makes an explicit `Observable<T>` type annotation on an interceptor's `intercept()` method fail to typecheck (already visible, unfixed, in `metrics/performance-monitoring.interceptor.ts`) — routed around it the same way `interceptors/logging.interceptor.ts` already does, casting through `any` at the `.pipe()` call rather than fixing the underlying rxjs dependency duplication (out of scope here). `public-profile.controller.ts`/`payments.controller.ts` still inject `PrismaService` directly into controllers — untouched, still tracked as a real but separate code-quality issue, not a security gap anymore now that the interceptor exists.

---

## P1 — Critical Path: Financial & Operational Integrity

### P1-01: Outbox Pattern for Payment → Wallet → Notification Writes
- **Priority**: P1
- **Status**: ✅ DONE
- **Owner**: Backend Team
- **Story Points**: 13
- **Implementation notes**:
  - New `OutboxEvent` Prisma model (migration `20260703000002_add_outbox_events`): `aggregateType`, `aggregateId`, `eventType`, `payload` (Json), `status` (PENDING/PROCESSING/PROCESSED/DEAD_LETTER), `retries`, `lastError`, `processedAt`.
  - New `backend/src/outbox/` module: `OutboxService` owns its own BullMQ `Queue`/`Worker` (plain `{host, port, password}` connection — deliberately not `RedisService.getConnection()`, which returns a `node-redis` client incompatible with BullMQ's expected `ioredis`-style connection, a separate latent bug found in the codebase's existing dead `job-queue.service.ts`). `createEventInTx(tx, input)` writes the outbox row inside the caller's existing `$transaction`; `enqueuePendingEvents()` atomically claims PENDING rows via a conditional `updateMany`; `dispatchEvent()` is idempotent against duplicate delivery and DEAD_LETTERs + Sentry-captures after 3 attempts.
  - `OutboxPollerService` (`@Cron(EVERY_MINUTE)`) drains the outbox — this only works because `ScheduleModule.forRoot()` was added to `app.module.ts`; it had never been registered anywhere, silently disabling every `@Cron` job app-wide (including the pre-existing `EscrowExpiryService`).
  - `wallet.service.ts::depositFunds` takes a 5th optional `notifyOnDeposit` param that writes the outbox event; wired into the Paystack/Flutterwave webhook handlers in `payments.service.ts`.
  - Admin visibility: `GET /admin/outbox-events`, `POST /admin/outbox-events/:id/retry`.
  - **Discovered along the way**: `NotificationService`/`NotificationPreferencesService` were injected by several services (`WalletService`, `PaymentsService`, and later found in `AppointmentsService`, `SubscriptionsService`, `ForumService`, `AdminService`, `GuidancePlansService`) but never actually provided by any of their modules — every one of those modules now provides them directly (see P1-03's build-fix notes below for the full list; this was a single systemic gap, not five separate bugs).
  - Tests: `outbox.service.spec.ts` (12), `outbox-poller.service.spec.ts` (3), plus a genuine Postgres-backed atomicity assertion added to `wallet.integration.spec.ts` (AC-2) proving the outbox row lands in the same transaction as the balance update.

### P1-02: Timeout + Retry/Backoff on Synchronous External Calls

- **Priority**: P1
- **Status**: ✅ DONE
- **Owner**: Backend Team
- **Story Points**: 5
- **Implementation notes**:
  - New `backend/src/utils/retry-with-backoff.util.ts`: shared `retryWithBackoff()` + classifiers (`isNetworkLevelFailure`, `isTimeout`, `isServerError`). 10 unit tests.
  - `whatsapp.service.ts`: 15s axios timeout, wrapped in `retryWithBackoff()`. 5 unit tests.
  - `paystack-api.service.ts`: 20s axios timeout. **Critical discovery**: `initializeClient()` was defined but never invoked from anywhere (no constructor call — it's async — and no lifecycle hook), so `this.client` was always `null` and every Paystack call always threw `"Paystack is not configured"` — Paystack could never have processed a single real transaction. Fixed by implementing `OnModuleInit`. Retry policy is differentiated: `verifyTransaction` (read-only) gets the full default policy; `initializeTransaction`/`createRefund` (mutating, money-moving) only retry on a *confirmed* network-level failure, explicitly not on timeout/5xx, since the request may have already reached and been processed by Paystack (double-charge/double-refund risk). 10 unit tests.
- **Dependencies**: P1-01
- **Notes**: The Paystack `initializeClient()` bug was arguably more severe than anything this story originally scoped for — it meant the platform's primary payment gateway was silently 100% non-functional.

### P1-03: Test Coverage Ramp-Up on Financial Flows

- **Priority**: P1
- **Status**: ✅ DONE — scope expanded significantly beyond the original story once real integration-test execution surfaced a much larger problem (see below)
- **Owner**: Full Team
- **Story Points**: 21 (actual effort well beyond original estimate — see notes)
- **What shipped**:
  - Backend Jest `coverageThreshold` wired as a real CI gate (`statements/branches/functions/lines`), raised twice this session as real coverage improved: 17/15/13/17 → **20/18/16/20** (current measured baseline: 20.6/18.56/16.77/20.38%, after both new tests and dead-code removal below).
  - CI workflow's unit-test step now runs with `--coverage`.
  - All 4 backend integration spec files verified passing end-to-end against a real Postgres: `wallet.integration.spec.ts` (10/10), `auth.integration.spec.ts` (18/18), `payment-idempotency.integration.spec.ts` (6/6, full rewrite — see below), `db-relationships.integration.spec.ts` (52/52, was previously self-skipping everywhere since `RUN_INTEGRATION_TESTS=1` was never set; now set in CI).
  - `payment-idempotency.integration.spec.ts` was **completely rewritten**: it was 100% built around `POST /api/wallet/deposit`, a route permanently removed in EMG-01. It now drives the actual current path — `POST /payments/webhook/paystack` with a real HMAC-SHA512 `x-paystack-signature` (EMG-02) — and asserts idempotent replay via direct Postgres checks (one `Transaction` row per gateway reference, wallet balance credited exactly once) rather than trusting response-body shape.
  - `users.service.spec.ts` and `notifications/onboarding-email.service.spec.ts`: both had genuine assertion drift vs. actual service behavior (wrong exception type expected — `ForbiddenException` vs. the real `UnauthorizedException`; wrong Prisma call shape — `select` vs. actual `include`; wrong stale imports — `./mail.service` doesn't exist, real dependency is `EmailService`). Rewritten to match real behavior, not the other way around.
  - `backend/test/mocks/common.mock.ts` (used in place of the real `@ile-ase/common` package for unit tests) was missing ~24 enums that real code imports (`CourseLevel`, `AdminSubRole`, `OrderStatus`, `VerificationTier`, etc.) — invisible under the unit config's `isolatedModules` transpile-only mode, but fatal once a full-program type-check tried to load `AppModule`. Backfilled from the real enum sources.
- **The actual story here — a broken production build**: fixing the integration tests required getting `npm run build` (full-program `tsc`, not the unit tests' `isolatedModules` transpile-only mode) to pass, and it was failing with **164 TypeScript errors** across 9 modules that were registered in `app.module.ts` but had zero consumers anywhere else in the codebase (frontend or backend): `elders/`, `compliance/`, `rbac/`, `moderation/`, `legal/`, `performance/` (4 controllers), `alerts/`, and `gdpr/consent.*` (a duplicate, unused parallel implementation — the real, frontend-used GDPR consent flow lives entirely in `gdpr.service.ts` and never touched `ConsentService`). Each referenced Prisma models/fields that never existed (`prisma.permission`, `prisma.rolePermission`, `ForumThread.lockedById`, `Temple.admins`, etc.) — a large batch of aspirational scaffolding that was wired into the app entrypoint to look complete but was never functional and never could have compiled under a real build. All 9 were deleted (same treatment as `enhanced-practitioner-onboarding` in P0-03's audit) after confirming zero reachability from any controller path the frontend actually calls. `npm run build` now passes with 0 errors.
  - Also fixed along the way: `PushNotificationService`'s constructor threw synchronously (crashing app boot entirely) whenever Firebase env vars were unset — now logs a warning and disables push notifications gracefully, matching the `PaystackApiService`/`isConfigured()` pattern from P1-02. `metrics.controller.ts` was missing the `SkipThrottle` import it decorated with. `performance-monitoring.middleware.ts` called three `EnhancedMetricsService` methods that never existed (`recordRequest`/`recordResponseTime`/`recordStatusCode` — replaced with the one method that already covers all three, `recordHttpRequest`, plus wiring in the missing `recordRequestEnd` so the in-flight gauge actually decrements). A duplicate, unregistered `PerformanceMonitoringInterceptor` and a duplicate, unregistered `NotificationProcessor` (its one `'sendPush'` job type called a nonexistent `PushNotificationService.executeSendPush` — but the whole class was never provided by any module, so none of it ever ran) were deleted as confirmed-dead duplicates of the live middleware/inline-send paths.
  - **Real, previously-invisible bugs found and fixed via this testing effort** (not test-file bugs — actual production defects):
    1. **Self-service authorization was silently broken everywhere**: `JwtStrategy.validate()` never set `sub` on the returned user payload, but `UsersController.findOne/update/completeOnboarding` and `ClientSessionNotesService` (12 call sites total) all authorize ownership via `currentUser.sub`. Every non-admin self-access check always evaluated `undefined !== id` → always true → always 403. A CLIENT could never view/update their own profile or session notes via these routes. Fixed by setting `sub: user.id` in `JwtStrategy.validate()`.
    2. **Every error response in production has used Nest's bare default shape**, never the frontend's expected `{ success: false, error: StandardApiError }` (which `frontend/src/shared/utils/api-error.ts` has been written against since PB-202.5). Three separate global exception filters existed (`GlobalExceptionFilter`, `SentryExceptionFilter`, `FriendlyExceptionFilter`) and *none* was ever registered via `app.useGlobalFilters()`. Kept the one with the richest Prisma/JWT error mapping, added Sentry reporting to it (merging in what the now-deleted `SentryExceptionFilter` did), registered it in `main.ts`, and deleted the two redundant duplicates (plus the now-orphaned `FriendlyErrorHandlerService`/`ErrorHandlingModule`).
    3. Several modules never imported the module providing a service they inject (`AdminModule` missing `VerificationModule`/`WalletModule`/`PaymentsModule`/`CirclesModule`; `AppointmentsModule` missing `WalletModule`/`WhatsAppModule`; `UsersModule` providing `CacheManagerService` directly instead of importing `CacheModule`, leaving its `RedisCacheService` dependency unresolved; `DocumentsModule`'s `SecurityModule` import never actually provided `VirusScanService`) — none of this was catchable by the unit test suite's `isolatedModules` config; only became visible once `Test.createTestingModule({ imports: [AppModule] })` could even compile.
- **Dependencies**: P0-01 ✅
- **Notes**: This is the clearest example in the whole backlog of the manifesto's core thesis — a green unit-test suite (`isolatedModules: true`, mocked Prisma throughout) coexisted with a production build that could not compile, an app that could crash at boot depending on env vars, an authorization check that always failed, and an error-response contract the frontend has never actually received. None of this was a one-line typo; all of it was invisible until something forced the full program graph to actually run.

### P1-04: Schema Hygiene — Remove Out-of-Band Schema Files

- **Priority**: P1
- **Status**: ✅ DONE
- **Owner**: Backend Team
- **Story Points**: 2
- **Implementation notes**:
  - `backend/prisma/schema_fixed.prisma` diffed against `schema.prisma` and confirmed to be a stale, already-superseded snapshot (it even still contained an old `PlatformAnnouncement` model later consolidated into `Announcement`) — deleted. `temp_schema.txt` was not present in the repo as scanned; no action needed.
  - New CI job `schema-hygiene` in `ci-cd.yml`: fails if any `.prisma`/`.txt` file other than `schema.prisma` exists in `backend/prisma/`.
- **Dependencies**: P0-01 ✅

---

## P2 — Structural Hardening

### P2-01: Frontend Anti-Corruption Layer (DTO Mapping)
- **Priority**: P2
- **Status**: ❌ NOT STARTED
- **Owner**: Frontend Team
- **Story Points**: 8
- **Description**: The frontend has no dedicated type-mapping layer between API responses and view models — it consumes shared domain types directly (`UserRole`, `AdminSubRole` from `@common`), and `use-auth.ts` casts demo data `as any` into the same `User` type used for real API responses. A backend field rename has no compiler boundary to catch it before it reaches a component.
- **Acceptance Criteria**:
  - [ ] `frontend/src/types/api/` introduced with explicit response-shape types per endpoint, separate from `@common` domain types
  - [ ] API client layer maps raw responses into these types at the boundary (single place to update on backend changes)
  - [ ] `as any` casts in `use-auth.ts` removed — demo data conforms to the same mapped type or is clearly isolated behind a `DemoUser` type that cannot silently satisfy `User`
- **Dependencies**: P0-04 (backend DTOs should exist first so frontend types can mirror them, not the raw Prisma shape)
- **Notes**: This is the frontend half of VCM-03/33 — the backend DTO layer (P0-04) and this task together close the full leak path from DB row to UI prop.

### P2-02: Retire or Fully Remove `VITE_DEMO_MODE`
- **Priority**: P2
- **Status**: ✅ DONE — product decision: **keep and formalize** (demo/quick-access mode is a permanent dev/QA feature, not legacy cruft)
- **Owner**: Frontend Team
- **Story Points**: 5
- **Implementation notes**:
  - New `frontend/src/shared/utils/dev-mode.ts` exports `isDevModeActive()` — the single point of truth, wrapping `!!localStorage.getItem('dev_mode_role')` in `import.meta.env.DEV`. Since Vite/Rollup statically replace and tree-shake `import.meta.env.DEV` in production builds, this branch cannot exist in a production bundle at all, regardless of what ends up in a user's localStorage.
  - All 60 `!localStorage.getItem('dev_mode_role')` read sites across 44 files replaced with `!isDevModeActive()` (mechanical, verified by diffing before/after — same call sites, same logic, just routed through the DEV-gated helper). `CacheDebugger.tsx`'s `localStorage.getItem('dev_mode_role') !== null` form converted the same way.
  - `use-auth.ts`'s one *value* read (`const devRole = localStorage.getItem('dev_mode_role')` inside the rehydration `useEffect`) was left untouched — it already sits behind an explicit `if (process.env.NODE_ENV === 'production') return;` guard on the line above, so it was already safe; converting it to the boolean-only helper would have lost the actual role value it needs.
  - `sidebar-layout.tsx`'s separate `VITE_DEMO_MODE`/`VITE_ENABLE_DEMO_MODE` env-var check wrapped in `import.meta.env.DEV &&` for the same reason.
  - `vite-env.d.ts`'s `VITE_DEMO_MODE`/`VITE_ENABLE_DEMO_MODE` type declarations restored (had been commented out, describing a `disabled in production` claim the code didn't actually enforce).
  - CLAUDE.md's "Demo Data Cleanup — ✅ COMPLETE" section rewritten to distinguish the March 24 pass (real fallback-data removal from user-facing views — genuinely done, unrelated) from the separate quick-access/`devLogin()` mechanism (now formalized, not removed).
  - Did **not** delete `demo/demo-ecosystem.ts`, `demo/profiles/users.ts`, `features/messages/demo-messages.ts`, or `QuickAccessPage.tsx` — those back the kept feature by design.
  - Verified zero regressions: frontend `tsc --noEmit` clean; full Vitest suite diffed before/after (`git stash` both runs) — identical 21 pre-existing failing test files, none newly broken.
- **Dependencies**: P0-02 ✅
- **Notes**: Asked the product owner directly rather than guessing — this was a genuine three-way fork (keep-and-gate / fully-delete / defer) that code archaeology alone couldn't resolve.

### P2-03: Timezone-Aware Scheduling Fields
- **Priority**: P2
- **Status**: ✅ DONE
- **Owner**: Backend Team
- **Story Points**: 8
- **Real bug this surfaced**: `appointments.service.ts::createBooking` validated "is this appointment in the future" via `` new Date(`${date}T${time}`) <= new Date() `` — a date-time string with no offset is parsed by the JS `Date` constructor as the *server's* local time, not the appointment's `timezone`. For the default `Africa/Lagos` (UTC+1) on a server running in UTC (the normal case), every booking's future-check and every conflict/overlap check (`timesOverlap`) was silently off by an hour. This wasn't hypothetical — verified directly: `combineDateTimeInZone('2026-08-01','14:30','Africa/Lagos')` → `13:30:00Z`, while the old `new Date('2026-08-01T14:30')` on a UTC server returns `14:30:00Z`, a real hour of drift on every single booking.
- **Implementation notes**:
  - Added `date-fns-tz@1.3.8` (matching the existing `date-fns@2.30.0` major version already in use) rather than hand-rolling IANA timezone arithmetic.
  - New `backend/src/utils/scheduling.util.ts`: `combineDateTimeInZone(date, time, timezone)` — the single point of truth for this conversion, `formatScheduledAt()` for the reverse direction. 6 unit tests, including a DST-crossing case (`America/New_York` in January vs. August) and a same-wall-clock-different-instant regression guard.
  - Migration `20260704000001_add_scheduled_at` adds `scheduledAt DateTime? @db.Timestamptz` to `Appointment` and `TutorSession` and backfills it from existing `date`+`time`+`timezone` — **per-row inside a `DO $$` exception-handling loop**, not a single bulk `UPDATE`, so any pre-existing row with a malformed date/time string or an unrecognized timezone name is left with `scheduledAt = NULL` (logged via `RAISE WARNING`) instead of aborting the whole migration on a live financial platform's booking data. Verified directly against Postgres with deliberately malformed rows before removing them.
  - Field is nullable, not `NOT NULL` as originally scoped — deliberately more conservative for a live-money-adjacent migration whose real backfill success rate against production data couldn't be verified from this environment. All new code paths always populate it; tightening to `NOT NULL` is a safe follow-up once 100% backfill is confirmed in production.
  - `appointments.service.ts`: `createBooking` computes and stores `scheduledAt`; the future-date check and `timesOverlap`'s conflict detection both now use it (`existing.scheduledAt ?? combineDateTimeInZone(...)` for pre-migration rows); `update()` recomputes `scheduledAt` whenever `date`/`time`/`timezone` changes; `checkAvailability` gained a `timezone` field on `CheckAvailabilityDto` and uses the same timezone-aware future-check.
  - `tutors.service.ts::createTutorSession` populates `scheduledAt` on create (same pattern).
  - `dashboard.service.ts`: the `scheduledDate: new Date(\`${apt.date}T${apt.time}\`)` construction (client and babalawo dashboard summaries) replaced with a `resolveScheduledAt()` helper preferring the real column.
  - `babalawo-client.service.ts::getClientTimeline`: same fix for the merged appointment/guidance-plan relationship timeline.
  - Frontend: `shared/utils/format-scheduled-at.ts` (`Intl.DateTimeFormat` with an explicit `timeZone`, so display is correct regardless of the *viewer's* browser timezone) wired into `BookingConfirmation.tsx`. `client-consultations-view.tsx`'s upcoming/completed filtering — previously `` new Date(`${a.date}T${a.time}`) > now ``, the same server/browser-local-time bug on the client side — now prefers `scheduledAt` too.
  - Did not touch: `dashboard.service.ts`'s week-bucketing trend chart (`new Date(apt.date)`, date-only string — correctly parsed as UTC per spec, no time component involved, low-precision aggregate use case) and `prescriptions.service.ts`'s 30-day-overdue check (same date-only, non-bug pattern). Left 9 of the frontend's originally-flagged files alone after confirming their `.date`/`.time` references were unrelated (different fields, e.g. `createdAt`) once actually inspected — only 3 frontend files had the real bug.
- **Dependencies**: P0-01 ✅, P0-03 ✅
- **Notes**: Verified via a real Postgres run (not just unit tests): inserted two valid appointments (`Africa/Lagos` and `America/New_York`, the latter during EDT) plus two deliberately malformed rows, ran the actual migration SQL, and confirmed both valid conversions matched the `date-fns-tz` computation exactly and both malformed rows were safely skipped with a warning rather than aborting.

### P2-04: File Size Reduction on Remaining Monoliths
- **Priority**: P2
- **Status**: 🟡 IN PROGRESS — the previously-flagged 12,481-LOC file is already gone
- **Owner**: Full Team
- **Story Points**: 8
- **Description**: `BACKLOG_STATUS.md` set a target of "all files under 1,000 LOC." Five files still exceed it: `forum.service.ts` (2437), `admin.service.ts` (2014), `thread-view.tsx` (1360), `onboarding-view.tsx` (1209), `App.tsx` (1114).
- **Acceptance Criteria**:
  - [ ] `forum.service.ts` split by responsibility (threads, posts, moderation, crisis-detection, search) into separate services behind a facade
  - [ ] `admin.service.ts` split by admin domain area (users, forum, finance, content) matching the existing `admin-*-tab.tsx` frontend split
  - [ ] `onboarding-view.tsx` fetch/validation logic extracted into a `useOnboarding` hook, leaving the component as render-only
  - [ ] `App.tsx` route table and top-level providers extracted from any remaining business logic
- **Dependencies**: P0-01, P1-03 (don't split large files without test coverage protecting behavior first)
- **Notes**: Do this after P1-03 gets meaningful test coverage on these files — splitting `forum.service.ts` untested risks silently changing crisis-detection or moderation behavior.

---

## P3 — Cleanup & Polish

### P3-01: Finish Console-to-Logger Migration on Frontend
- **Priority**: P3
- **Status**: 🟡 IN PROGRESS — `shared/utils/logger.ts` exists and is used in most places
- **Owner**: Frontend Team
- **Story Points**: 2
- **Description**: CLAUDE.md claims "all console replaced with logger," but ~23 files still call `console.log`/`console.error` directly, including `profile-page.tsx`, `preferences-context.tsx`, `use-performance-measure.ts`, and `error-handler.util.ts`.
- **Acceptance Criteria**:
  - [ ] Remaining `console.*` calls in the 23 identified files replaced with the existing `logger` utility
  - [ ] ESLint rule added (`no-console`) to prevent regressions, with an explicit override only inside `logger.ts` itself
- **Dependencies**: None
- **Notes**: Small, mechanical. Add the lint rule so this doesn't need re-auditing next quarter.

### P3-02: Config Extraction for Remaining Inline Literals
- **Priority**: P3
- **Status**: 🟢 MOSTLY DONE — most business literals already load from API/config
- **Owner**: Frontend Team
- **Story Points**: 1
- **Description**: Sentry sample rates (`shared/config/sentry.ts`) and a handful of animation-delay values are hardcoded inline. Low risk, but worth sweeping into the existing config pattern for consistency.
- **Acceptance Criteria**:
  - [ ] Sentry sample rates moved to env-driven config (already partially true for DSN — extend to sample rate)
  - [ ] Repeated animation-delay magic numbers (`0.1`, `0.15`) consolidated into a shared motion-constants file if reused across 3+ components
- **Dependencies**: None
- **Notes**: Genuinely minor — most of the sin #10 pattern from the manifesto (hardcoded fees/FX rates) does **not** apply here; this platform's business numbers are already config/API-driven.

### P3-03: CI Enforcement of Lockfile & Audit
- **Priority**: P3
- **Status**: ❌ NOT STARTED
- **Owner**: DevOps
- **Story Points**: 2
- **Description**: A root `package-lock.json` already makes builds reproducible (npm workspaces), but nothing currently enforces `npm ci` over `npm install` in deploy scripts, and `npm audit` isn't gated in CI.
- **Acceptance Criteria**:
  - [ ] Deploy scripts (`deploy.sh`, `deploy.ps1`, Dockerfiles) confirmed to use `npm ci`, not `npm install`
  - [ ] CI step runs `npm audit --workspaces` and fails on `high`/`critical` CVEs
  - [ ] Dependabot or Renovate configured for `backend`, `frontend`, and `common` workspaces
- **Dependencies**: P0-01 (needs CI, needs a repo)
- **Notes**: The lockfile already exists and is doing its job — this is about making sure the *process* around it can't quietly bypass it.

### P3-04: Purge Committed Debug Dumps and Close the `.gitignore` Gap
- **Priority**: P3
- **Status**: 🟡 MOSTLY DONE (July 3, 2026) — done exactly in the order this story insisted on: *before* `git init`, not after.
- **Owner**: Tech Lead
- **Story Points**: 1
- **Description**: 14 stray debug/lint/test-output files (~2.4MB, including a 1.75MB `test_output_6.txt`) sit in the working tree from what looks like a single bulk "dump everything" commit. Separately, `.gitignore` only excludes exact `.env`/`.env.local`/`.env.*.local` filenames — `.env.staging`, `.env.staging.local`, `scripts/.env.docker`, and `backend/.env` (all containing real-looking, non-placeholder-length secret values) fall outside any ignore rule.
- **Acceptance Criteria**:
  - [x] All 14 `*_output*.txt`, `errors.txt`, `*_debug.txt`, `prisma/temp_schema.txt` files deleted from the working tree before the first commit
  - [x] `.gitignore` updated to a wildcard `.env*` rule with exceptions — used `!**/.env*.example` (covers `backend/.env.example`, `frontend/.env.example`, and `scripts/.env.docker.example`, which the simpler `!.env.example` pattern originally planned would have missed) plus one explicit carve-out for `frontend/.env.production` (confirmed to hold only `VITE_`-prefixed values, which Vite bundles into the client and are public by construction — not a secret). Verified with `git check-ignore` against all 8 real `.env*` files: the 4 secret-bearing ones are ignored, the 4 safe ones are tracked
  - [ ] Rotate secrets in `.env.staging`/`.env.staging.local`/`scripts/.env.docker` — **not done, requires the platform owner**; these were never committed (confirmed via `git diff --cached --name-only` before the first commit), so rotation is precautionary rather than urgent, but the guidance to rotate on general exposure-hygiene grounds still stands
  - [ ] CI check to fail the build on a staged `.env*`/debug-dump file — not built (no CI workflow changes made this session)
- **Dependencies**: P0-01 (done, same session, correct order)
- **Notes**: Confirmed via `git diff --cached --name-only` before committing that only the 4 safe `.env*` files were staged, and separately swept the staged file list for other secret-shaped filenames (`.pem`, `credentials`, `id_rsa`, etc.) — the only hits were legitimate source files (a secrets-manager service module, a credentials-upload DTO, and a secret-rotation runbook document), not real leaked secrets.

### P3-05: Remove or Wire Up Orphaned Services; Type the Financial Write Path
- **Priority**: P3
- **Status**: ❌ NOT STARTED
- **Owner**: Backend Team
- **Story Points**: 5
- **Description**: `OpenSearchService` and `CdnService` (`backend/src/shared/services/`) are registered in the DI container but never injected or called anywhere in application code — dead infrastructure. Separately, of 448 `: any` type escapes in the codebase, the highest-risk concentration is in `payments.service.ts` and `wallet.service.ts` — webhook payloads and `$transaction` callbacks in exactly the files with the Tier 1/2 money bugs (EMG-01 through EMG-06) have no compile-time type protection.
- **Acceptance Criteria**:
  - [ ] `OpenSearchService`/`CdnService` either wired into an actual call site (if search/CDN functionality is planned near-term) or deleted along with their module registrations
  - [ ] Webhook payload types replaced with real interfaces matching Paystack's/Flutterwave's documented webhook schema instead of `any`
  - [ ] `$transaction` callback parameters in `wallet.service.ts` typed against the actual Prisma transaction client type instead of `any`
  - [ ] ESLint rule considered (`@typescript-eslint/no-explicit-any`) scoped at minimum to `payments/` and `wallet/` directories
- **Dependencies**: EMG-01 through EMG-07 (type the code once the logic bugs are already fixed, not before — avoid typing bugs into permanence)
- **Notes**: The `any` types aren't just style debt here — they're the reason the webhook-signature and idempotency bugs (EMG-02/EMG-03) were easy to introduce and easy to miss in review.

### P3-06: De-duplicate Enrollment/Booking Scaffold Logic
- **Priority**: P3
- **Status**: ❌ NOT STARTED
- **Owner**: Backend Team
- **Story Points**: 5
- **Description**: `academy.service.ts` (`createEnrollment`) and `tutors.service.ts` (`createTutorSession`) independently implement the identical "fetch parent → status check → conflict check → create-with-includes" pattern with slightly different exception messages and no shared test coverage for the common contract.
- **Acceptance Criteria**:
  - [ ] Shared helper/base class extracted for the "verify eligibility → check conflict → create" scaffold, parameterized by entity type
  - [ ] Both `academy.service.ts` and `tutors.service.ts` refactored to use the shared helper
  - [ ] One shared test suite covers the common contract (conflict detection, status gating) instead of two divergent ones
- **Dependencies**: P1-03 (needs test coverage in place before refactoring to avoid silently changing behavior)
- **Notes**: Low urgency on its own, but every future booking-like feature (circle events, video-call slots) is likely to copy-paste this scaffold a third time unless it's extracted now.

---

## Sprint Metrics

### Audit Sprint — Codebase Sin Scan (July 3, 2026)

- **Scope**: 58 backend modules, 102 services, 58 controllers, 45 backend spec files; 292 frontend components, 50 frontend test files; 73 Prisma models, 29+ migrations
- **Sins confirmed present**: 12 of 40 (Sins #9, #12, #13, #16, #17, #24, #25, #27, #30, #31, #33, #38, #40 — see full audit table below)
- **Sins fully clean**: 15 of 40 (indexes/FKs, rate limiting, health checks, secrets management, connection pooling, background worker resilience, structured backend logging, and others — see ✅ table above)
- **Highest-severity single finding (pattern-level scan)**: no git repository on a live production financial platform (VCM-38)
- **Highest-severity systemic finding (pattern-level scan)**: 28%/17% mock-heavy test coverage against payment and wallet flows (VCM-30)

### Harsh Adversarial Audit — Exploit Hunt (July 3, 2026, same day)

A second pass abandoned pattern-matching and read the actual money/auth code adversarially. Results were materially worse than the pattern-level scan suggested:

- **Scope**: `auth/`, `payments/`, `wallet/`, `marketplace/`, `subscriptions/`, `documents/`, `messaging/`, `appointments/`, `currency.service.ts`, plus a full-repo sweep for dead code, debug artifacts, and type-safety gaps
- **Exploitable-today vulnerabilities confirmed**: 7 (Tier 1 — wallet self-deposit, webhook signature bypass, webhook replay/double-credit, unrestricted refund, 2× document IDOR, wallet balance disclosure)
- **Race conditions confirmed under concurrency**: 3 (escrow debit TOCTOU, marketplace stock oversell, appointment double-booking)
- **Money-correctness logic bugs confirmed**: 4 (currency fallback=1 for non-USD/NGN, escrow never released on delivery, unbounded refund amount, non-idempotent refunds)
- **Dead weight confirmed**: 2 unused DI-registered services (`OpenSearchService`, `CdnService`), 448 `any` escapes concentrated in `payments.service.ts`/`wallet.service.ts`, ~2.4MB of committed debug dumps, a `.gitignore` gap exposing real-looking secrets in 4 `.env` variants
- **Highest-value single fix found**: `if (signature)` → mandatory signature check in `payments.service.ts:376,443` (EMG-02) — a two-line change that closes the webhook-forgery path feeding both free-money-minting (EMG-01) and double-crediting (EMG-03)
- **What held up under adversarial pressure**: mass-assignment protection, ADMIN self-registration blocking, CORS allowlisting, JWT secret boot-time validation, `messaging` module's ownership checks, S3 path-traversal protection, and marketplace server-side price calculation all remained correct when actively attacked, not just when pattern-matched

### Recommended Execution Order

```
✅ EMG-02 (webhook signature — 2-line fix, highest value in the backlog)
    → ✅ EMG-01 (wallet self-deposit) → ✅ EMG-03 (webhook idempotency)
    → ✅ EMG-04 (document IDOR) → ✅ EMG-05 (balance disclosure + refund authz)
    → ✅ EMG-06 (escrow/stock/booking races) → ✅ EMG-07 (currency fallback + escrow release)
    → ✅ P3-04 (purge debug dumps + fix .gitignore — done AS PART OF P0-01, before the first commit)
    → 🟡 P0-01 (git init done; remote/branch-protection/CI still need the platform owner)
    → ✅ P0-02 (dev-login bypass) → 🟡 P0-03 (soft delete — 3 named models done, audit table deferred) → 🟡 P0-04 (DTO layer — shipped a broader interceptor-based safety net instead of per-endpoint DTOs)
    → ✅ P1-01 (outbox) → ✅ P1-02 (timeouts/retry) → ✅ P1-03 (real test coverage — plus fixing the broken build it uncovered) → ✅ P1-04 (schema hygiene)
    → P2-* (structural hardening) → P3-01/02/03/05/06 (remaining cleanup)   ← NEXT UP
```

**Status as of July 4, 2026 (single session)**: everything through P1-04 has been executed in the recommended order, all ✅/🟡 above. Every fix was verified against automated tests (new or existing) before moving to the next item, and a full-suite regression check was run after each one. Unit-suite failure count held steady at the same pre-existing, unrelated 21 suites through P0, then **dropped to 19** during P1-03 as two of those pre-existing failures (`users.service.spec.ts`, `notifications/onboarding-email.service.spec.ts`) were fixed for real. All 4 backend integration spec files (86 tests total) now pass end-to-end against a real Postgres, and `npm run build` — previously failing with 164 TypeScript errors — now passes with 0. **P2 is next.**

**Rationale (unchanged)**: The EMG-* fixes protect real money and real user data *today* — they couldn't wait for git init, and were folded into the very first commit once the repo existed (P3-04's debug-dump purge and `.gitignore` fix went into that same first commit, so no secret or junk file was ever actually committed — verified via `git diff --cached` before committing). P0-01 gives every subsequent fix a reviewable diff and rollback path (though the remote/CI half still needs the platform owner). P0-02 closed the remaining exploitable client-side hole. P0-03 stopped ongoing data loss for the three named models. P0-04 closed the sensitive-field-leak vector platform-wide via a different mechanism than originally scoped, trading formal DTO-per-endpoint precision for immediate full coverage. P1 protects the money-moving path structurally (outbox, timeouts, real test coverage) next.

---

## Architecture Compliance — Vibe Coding Manifesto Audit (Full 40-Sin Scan)

Every row below was checked directly against the codebase at `/Users/a40/Documents/iluase` on July 3, 2026. ✅ = not present / well-mitigated. ⚠️ = confirmed present or only partially mitigated — tracked above. ◻︎ = not deeply audited this pass (noted, not fabricated).

| # | Sin | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Logic in Controller Layer | ⚠️ Partial | `public-profile.controller.ts` and parts of `payments.controller.ts` call `PrismaService` directly instead of delegating fully to services — see P0-04 |
| 2 | Fat Models / Anemic Services | ✅ Not anemic | `forum.service.ts`/`admin.service.ts` are large but contain real business logic, not CRUD passthroughs — flagged instead under file-size (P2-04) |
| 3 | Leaking DB Entities to Frontend | ⚠️ Confirmed | No `ClassSerializerInterceptor`, no DTO classes on most endpoints — see P0-04 |
| 4 | Direct Mutation of State References | ✅ Clean | `thread-view.tsx` uses functional `setQueryData` updates; no direct mutation found in spot checks |
| 5 | Side Effects Inside Pure Functions | ⚠️ Partial | `onboarding-view.tsx` mixes fetch/validation/UI in handlers — see P2-04 |
| 6 | Slob Generic Marketing Landing Page | ◻︎ Not audited | Not covered in this pass — recommend a follow-up pass on the public marketing surface |
| 7 | Linear Happy-Path-Only Onboarding | ◻︎ Not audited | Ghost-record risk specifically was checked (see #8); full onboarding-edge-case audit not performed |
| 8 | Ghost Record Creation During Staged Registration | ✅ Clean | `onboarding-view.tsx` patches the existing user progressively; no per-step record creation found |
| 9 | Client-Side Only Enforcement of Security | ⚠️ Confirmed, High | Dev-login bypass (`QuickAccessPage.tsx`, `use-auth.ts`) ships without a route-level prod guard — see P0-02. Harsh audit found this sin also has a **server-side** cousin: `documents.service.ts` authorizes off the attacker-supplied route param instead of the JWT identity (EMG-04) — the enforcement gap isn't only client-side |
| 10 | Hardcoding Magic Numbers | ⚠️ Minor only | Business literals are mostly config/API-driven; only cosmetic constants (sample rates, animation delays) remain — see P3-02 |
| 11 | Silent N+1 Query Cascade | ✅ Not confirmed | Spot-checked `dashboard.service.ts` — single `findMany` then in-memory loop, no query-per-iteration pattern found |
| 12 | Blind Schema Abuse via JSON Dumps | ⚠️ Confirmed | 17 `Json`/`Json?` fields for structured relational data (order items, availability, votes) |
| 13 | Destructive Row Purging over Soft Deletes | ⚠️ Confirmed, High | Only `RefreshToken.deletedAt` exists across 73 models; 29 hard-delete call sites — see P0-03 |
| 14 | Using Transactional DB as Message Queue | ✅ Clean | BullMQ + Redis used properly for async jobs |
| 15 | Missing Index Constraints | ✅ Clean | 362 `@@index`/`onDelete` occurrences in schema |
| 16 | Timezone-Naive Timestamp Persistence | ⚠️ Confirmed | `Appointment`/`TutorSession` store `date`/`time` as `String` — see P2-03 |
| 17 | Manual Migrations via GUI / Out-of-Band Schema Edits | ⚠️ Confirmed | Stray `schema_fixed.prisma`, `temp_schema.txt` alongside canonical schema — see P1-04 |
| 18 | Catch-All Empty Exception Swallowing | ✅ Mostly clean | Catch blocks generally log via `Logger`; one intentional low-risk fire-and-forget swallow in `users.service.ts` |
| 19 | Unstructured Plain-Text Logging | ⚠️ Partial (frontend) | Backend clean (NestJS `Logger`/Winston); ~23 frontend files still use raw `console.*` — see P3-01 |
| 20 | Infinite Retries without Backoff and Jitter | ⚠️ Partial | BullMQ jobs have backoff; direct WhatsApp/Paystack axios calls have none — see P1-02 |
| 21 | Hardcoding Secret Keys | ✅ Clean | No hardcoded secrets found; `.env`/Secrets Manager used correctly |
| 22 | Absent Database Connection Pools | ✅ Clean | `connection_limit=10&pool_timeout=10` configured on `DATABASE_URL` |
| 23 | Blindly Deploying Premature Caching Layers | ✅ Reasonable | Redis cache used for hot paths (user profiles); no evidence of premature/incorrect caching |
| 24 | Fully Synchronous Processing of Unreliable 3rd-Party APIs | ⚠️ Confirmed | WhatsApp/Paystack calls awaited synchronously with no timeout — see P1-02 |
| 25 | Unpinned and Untracked System Dependencies | ⚠️ Partial | All `package.json` ranges are caret/tilde, but a root lockfile (npm workspaces) exists and pins effective versions — see P3-03 |
| 26 | Premature Microservice Fragmentation | ✅ Reasonable | 58 modules in one NestJS monolith — modularization matches genuine domain breadth, not literal microservices |
| 27 | Abandoned Feature Flag Code Trails | ⚠️ Confirmed | `VITE_DEMO_MODE` half-removed (commented out of types, still read at runtime) — see P2-02 |
| 28 | Blindly Pasting Unvetted Generative Logic | ◻︎ Not audited | No specific review checklist for AI-assisted code found; not independently verifiable in this pass |
| 29 | Complete Absence of Rate-Limiting Thresholds | ✅ Clean | `ThrottlerModule` + `@Throttle` applied on payments/auth controllers |
| 30 | Over-Mocking inside Unrealistic Test Frameworks | ⚠️ Confirmed, High | ~28% backend / ~17% frontend file-level test coverage, mock-heavy, no testcontainers evidence — see P1-03 |
| 31 | Distributing Transactions Across Disparate Networks (no Saga) | ⚠️ Confirmed, Critical | No outbox table; notification sends happen outside the wallet `$transaction` — see P1-01. Harsh audit found the sharper version of this sin: the payment↔wallet leg itself has no idempotency (webhook retries double-credit, EMG-03) and the escrow↔wallet leg has a TOCTOU race that goes negative (EMG-06) — this is not just a missing-notification problem, it's an inconsistent-money problem |
| 32 | Storing Highly Sensitive Credentials as Plain Text | ✅ Clean | Encryption service present; no plaintext credentials found |
| 33 | Direct Coupling of Frontend Structures to API Schema | ⚠️ Confirmed | No frontend DTO-mapping layer; shared domain types consumed directly — see P2-01 |
| 34 | Lack of Health Checks and Liveness Diagnostics | ✅ Clean | Terminus health checks with DB ping + custom metrics |
| 35 | Unmonitored Background Thread Workers | ✅ Clean | BullMQ jobs configured with attempts/backoff/bounded retention |
| 36 | Hardcoding Monolithic Environmental Assumptions | ✅ Reasonable | Stateless design, file storage delegated to S3 |
| 37 | Neglecting Cascading Database Foreign Key Constraints | ✅ Clean | `onDelete: Cascade` consistently applied on core relations |
| 38 | Committing Large-Scale Feature Monoliths to Mainlines (no VCS at all) | ⚠️ Confirmed, Critical | No `.git` directory exists anywhere in the project — see P0-01 |
| 39 | Completely Missing Database Connection Timeout Directives | ⚠️ Partial | DB pool timeout configured; outbound HTTP calls (WhatsApp/Paystack) have no timeout — see P1-02 |
| 40 | Silently Ignoring Failed Asynchronous System Writes | ⚠️ Confirmed | WhatsApp send failures caught, logged, and dropped by design with no DLQ/alert — see P0-04/P1-01 |

**Score: 12 confirmed sins, 3 not independently audited (#6, #7, #28), 25 clean or reasonably mitigated.** For a live platform moving real money with no version control until today, that is a materially better structural baseline than the "vibe-coded" archetype the manifesto describes — the gaps are concentrated, not systemic, and P0/P1 above close the ones that actually threaten data integrity or user trust.

**Revised verdict after the harsh adversarial pass:** the pattern-level score above is the optimistic read. Reading the actual money and access-control code adversarially (🔴🔴 EXPLOITABLE NOW, above) found that at least three of the "clean" architectural boxes — rate limiting ✅, secrets management ✅, RBAC guards ✅ — are true at the *pattern* level while the *specific* endpoints handling money and private documents (`wallet.deposit`, `payments.webhook`, `payments.refund`, `documents.getSignedUrl/deleteDocument`, `wallet.balance`) each have an individually-exploitable authorization or verification gap that a sin-pattern grep does not surface, because the code *looks* guarded (a controller, a service, a DTO exist) without the guard actually checking the right thing. **The honest summary is: the architecture is sound, but at least 7 of its load-bearing joints — all in payments, wallet, and documents — are unlocked right now.** Treat the 🔴🔴/EMG-* section as binding, not aspirational, and patch it before the next payment runs through the platform.
