/**
 * Shared stagger-delay constants for framer-motion `transition={{ delay }}`
 * fade-in sequences (P3-02). Consolidates the delay values reused across 3+
 * components instead of each one hardcoding its own copy of the same numbers.
 */
export const STAGGER_DELAY_1 = 0.1;
export const STAGGER_DELAY_2 = 0.15;
export const STAGGER_DELAY_3 = 0.2;
export const STAGGER_DELAY_4 = 0.3;
